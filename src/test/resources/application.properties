# Database Properties
spring.datasource.url                   = jdbc:postgresql://localhost:5432/fmzenglishtrainer
spring.datasource.username              = fmzenglishtrainer
spring.datasource.password              = fmzenglishtrainer$001*

# Flyway Properties
spring.flyway.sql-migration-prefix      =
spring.flyway.sql-migration-separator   = -
spring.flyway.sql-migration-suffixes    = .sql
spring.flyway.validate-migration-naming = false
spring.flyway.validate-on-migrate       = false
spring.flyway.default-schema            = databasemigration
spring.flyway.table                     = patch_sql_history